/**
 * Package documentation for `com.mechforge.core.engine`.
 *
 * The engine package holds the calculator framework (definitions, input specs,
 * validation, results) shared by every MechForge calculator. Engineering formulas
 * live in `com.mechforge.core.calcs` — never in the UI layer.
 */
package com.mechforge.core.engine
