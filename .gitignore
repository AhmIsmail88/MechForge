# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
out/

# OS
.DS_Store
Thumbs.db

# Kotlin
.kotlin/

# Local config
local.properties

# Verification artifacts (regenerated by tools/verification)
tools/verification/engine_dump.jsonl

# Local backup of files replaced during development (not part of the build)
.backup-desktopmain-duplicates/

# Signing material (never commit private keys or their passwords)
signing/*.jks
signing/keystore.properties

# Build artifacts (APKs are shared via dist/, never tracked)
*.apk

# Shareable build drop
dist/

# Local project archive (not part of the sources)
MechForge.rar
